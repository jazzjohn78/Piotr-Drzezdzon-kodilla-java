package com.kodilla.good.patterns.challenges;

public class ProductOrderProcessor implements OrderProcessor {
    public boolean order(User user, Product product, Double quantity) {
        //order code here
        return true;
    }
}
